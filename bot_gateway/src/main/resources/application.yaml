server:
  port: 8080

spring:
  application:
    name: bot_gateway // 微服务名字

  cloud:
    nacos:
      discover:
        server-addr: 127.0.0.1:8848 // nacos地址 端口
    stream:
      rocketmq:
        binder:
          name-server: 127.0.0.1:9090 //RocketMq 地址